import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  contentContainer: {
    paddingVertical: 16,
    paddingTop: 0,
  },

  separator: {
    height: 16,
  },

  sectionContainer: {
    marginTop: 24,
    marginBottom: 12,
  },

  section: { flex: 1 },
});
