export type RecoveryReason = {
  id: string;
  title: string;
  description: string;
  icon: any;
};

export type Timeline = {
  id: string;
  title: string;
  subtitle: string;
  icon: any;
  iconColor: string;
};

export type Feeling = {
  id: string;
  title: string;
  subtitle: string;
  emoji: string;
};

export type Goal = {
  id: string;
  title: string;
  subtitle: string;
  icon: any;
};

export type Feature = {
  id: string;
  title: string;
  description: string;
  icon: any;
};
