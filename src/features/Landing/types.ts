export type RecoveryReason = {
  id: string;
  title: string;
  description: string;
  icon: any;
};

export type Timeline = {
  id: string;
  title: string;
  subtitle: string;
  icon: any;
  iconColor: string;
};
